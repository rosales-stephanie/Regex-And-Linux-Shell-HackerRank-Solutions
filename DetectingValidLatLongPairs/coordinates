#! /bin/sh
awk '{

if (NR != 1) {
    if ($0 ~ /\([+|-]?(90(\.0+)?|([0-9]|[1-8][0-9])(\.[0-9]+)?), [+|-]?(180(\.0+)?|([0-9]|1[0-7][0-9]|[1-9][0-9])(\.[0-9]+)?)\)/ )
    {
        print $0 "Valid"
    } else {
        print $0 "Invalid"
    }
}

}' testCoordinates
